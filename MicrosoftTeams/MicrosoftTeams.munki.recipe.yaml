---
Identifier: com.github.woodleighschool.munki.MicrosoftTeams
ParentRecipe: com.github.rtrouton.download.microsoftteams
MinimumVersion: "2.7"

Input:
  MUNKI_CATEGORY: Productivity
  NAME: Microsoft Teams
  pkginfo:
    blocking_applications:
      - Microsoft Teams (work or school)
      - Microsoft Teams
    category: "%MUNKI_CATEGORY%"
    description: >-
      Microsoft Teams is your modern solution for seamless collaboration and communication
      in the digital workplace. This platform brings together chat, video meetings, file collaboration,
      and integrated app functionalities in one place.
    developer: Microsoft
    name: "%NAME%"
    unattended_install: true
    uninstall_method: ""
    uninstallable: false

Process:
  - Processor: FlatPkgUnpacker
    Arguments:
      destination_path: "%RECIPE_CACHE_DIR%/unpacked"
      flat_pkg_path: "%pathname%"
      purge_destination: true

  - Processor: PkgPayloadUnpacker
    Arguments:
      destination_path: "%RECIPE_CACHE_DIR%/payload/Applications"
      pkg_payload_path: "%RECIPE_CACHE_DIR%/unpacked/MicrosoftTeams_app.pkg/Payload"
      purge_destination: true

  - Processor: Versioner
    Arguments:
      input_plist_path: "%RECIPE_CACHE_DIR%/payload/Applications/%NAME%.app/Contents/Info.plist"
      plist_version_key: CFBundleShortVersionString

  - Processor: PkgCopier
    Arguments:
      pkg_path: "%RECIPE_CACHE_DIR%/%NAME%-%version%.pkg"
      source_pkg: "%pathname%"

  - Processor: MunkiInstallsItemsCreator
    Arguments:
      derive_minimum_os_version: true
      faux_root: "%RECIPE_CACHE_DIR%/payload"
      installs_item_paths:
        - /Applications/Microsoft Teams.app

  - Processor: MunkiPkginfoMerger

  - Processor: com.github.woodleighschool.processors/SwiftIconExtractor
    Arguments:
      input_path: "%RECIPE_CACHE_DIR%/payload/Applications/%NAME%.app"

      NAME: "%NAME%"
      output_dir: "%RECIPE_CACHE_DIR%"
  - Processor: PathDeleter
    Arguments:
      path_list:
        - "%RECIPE_CACHE_DIR%/unpacked"
        - "%RECIPE_CACHE_DIR%/payload"

  - Processor: com.github.woodleighschool.woodstar.processors/WoodstarMunkiImporter
    Arguments:
      icon_path: "%RECIPE_CACHE_DIR%/%NAME%.png"
      pkg_path: "%pkg_path%"
      targets:
        exclude: []
        include:
          - actions:
              - optional_installs
            label_name: All Hosts
            package:
              strategy: latest

  - Processor: com.github.woodleighschool.woodstar.processors/WoodstarMunkiPackageCleaner
    Arguments:
      keep_version_count: 1
