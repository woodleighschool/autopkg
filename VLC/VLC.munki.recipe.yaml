---
Description: Downloads the latest version of VLC and imports it into Munki.
Identifier: com.github.woodleighschool.munki.VLC
ParentRecipe: com.github.autopkg.download.VLC

MinimumVersion: "1.1.0"

Input:
  ARCHITECTURE: arm64
  MUNKI_CATEGORY: Media
  MUNKI_REPO_SUBDIR: apps/%NAME%
  NAME: VLC
  pkginfo:
    blocking_applications:
      - VLC
    catalogs:
      - testing
    category: "%MUNKI_CATEGORY%"
    description: >-
      VLC is a free and open source cross-platform multimedia player and
      framework that plays most multimedia files, and various streaming
      protocols.
    developer: VideoLAN
    display_name: "%NAME%"
    icon_name: "%NAME%.png"
    name: "%NAME%"
    unattended_install: true
    unattended_uninstall: true
    uninstall_method: remove_copied_items
    uninstallable: true

Process:
  - Processor: Copier
    Arguments:
      destination_path: "%RECIPE_CACHE_DIR%/payload/Applications/%NAME%.app"
      overwrite: true
      source_path: "%pathname%/%NAME%.app"

  - Processor: MunkiInstallsItemsCreator
    Arguments:
      derive_minimum_os_version: true
      faux_root: "%RECIPE_CACHE_DIR%/payload"
      installs_item_paths:
        - /Applications/%NAME%.app

  - Processor: MunkiPkginfoMerger

  - Processor: MunkiImporter
    Arguments:
      munkiimport_appname: "%NAME%.app"
      pkg_path: "%pathname%"
      repo_subdirectory: "%MUNKI_REPO_SUBDIR%"

  - Processor: com.github.woodleighschool.processors/MunkiIconExtractor
    Arguments:
      NAME: "%NAME%"
      pathname: "%RECIPE_CACHE_DIR%/payload/Applications/%NAME%.app"

  - Processor: PathDeleter
    Arguments:
      path_list:
        - "%RECIPE_CACHE_DIR%/payload"
