---
Identifier: com.github.woodleighschool.munki.EasyFind
ParentRecipe: com.github.woodleighschool.download.EasyFind
MinimumVersion: "3.0"

Input:
  MUNKI_CATEGORY: Utilities
  NAME: EasyFind
  pkginfo:
    blocking_applications:
      - EasyFind
    category: "%MUNKI_CATEGORY%"
    description: >-
      Spotlight is great, but sometimes you need something more specialized.
      EasyFind finds files and folders by name, content, tags, or comments
      using advanced Boolean operators, wildcards, phrases, even regular
      expressions. It does not require indexing, is fast, and uses very little
      memory.
    developer: DEVONtechnologies
    name: "%NAME%"
    unattended_install: true
    unattended_uninstall: true
    uninstall_method: remove_copied_items
    uninstallable: true

Process:
  - Processor: PlistReader
    Arguments:
      info_path: "%RECIPE_CACHE_DIR%/expanded/EasyFind.app/Contents/Info.plist"
      plist_keys:
        CFBundleShortVersionString: version

  - Processor: com.github.woodleighschool.processors/SwiftIconExtractor
    Arguments:
      input_path: "%RECIPE_CACHE_DIR%/expanded/EasyFind.app"

  - Processor: DmgCreator
    Arguments:
      dmg_path: "%RECIPE_CACHE_DIR%/EasyFind-%version%.dmg"
      dmg_root: "%RECIPE_CACHE_DIR%/expanded"

  - Processor: PathDeleter
    Arguments:
      path_list:
        - "%RECIPE_CACHE_DIR%/expanded"

  - Processor: com.github.woodleighschool.woodstar.processors/WoodstarMunkiImporter
    Arguments:
      icon_path: "%icon_path%"
      munkiimport_appname: EasyFind.app
      pkg_path: "%RECIPE_CACHE_DIR%/EasyFind-%version%.dmg"
      targets:
        exclude: []
        include:
          - actions:
              - optional_installs
              - managed_updates
            label_name: All Hosts
            package:
              strategy: latest

  - Processor: com.github.woodleighschool.woodstar.processors/WoodstarMunkiPackageCleaner
    Arguments:
      keep_version_count: 1
