---
Identifier: com.github.woodleighschool.intune.Plinth
ParentRecipe: com.github.woodleighschool.download.Plinth
MinimumVersion: "3.0"

Input:
  description: >-
    Plinth is a managed kiosk browser for Woodleigh School library iMacs.
  NAME: Plinth
  publisher: Woodleigh School

Process:
  - Processor: FlatPkgUnpacker
    Arguments:
      destination_path: "%RECIPE_CACHE_DIR%/unpacked"
      flat_pkg_path: "%pathname%"
      purge_destination: true

  - Processor: PkgPayloadUnpacker
    Arguments:
      destination_path: "%RECIPE_CACHE_DIR%/payload"
      pkg_payload_path: "%RECIPE_CACHE_DIR%/unpacked/Payload"
      purge_destination: true

  - Processor: PlistReader
    Arguments:
      info_path: "%RECIPE_CACHE_DIR%/payload/Applications/%NAME%.app/Contents/Info.plist"
      plist_keys:
        CFBundleIdentifier: bundle_id
        CFBundleShortVersionString: bundle_version

  - Processor: com.github.woodleighschool.processors/SwiftIconExtractor
    Arguments:
      NAME: "%NAME%"
      output_dir: "%RECIPE_CACHE_DIR%"
      overwrite: true
      pathname: "%RECIPE_CACHE_DIR%/payload/Applications/%NAME%.app"

  - Processor: PathDeleter
    Arguments:
      path_list:
        - "%RECIPE_CACHE_DIR%/unpacked"
        - "%RECIPE_CACHE_DIR%/payload"

  - Processor: com.github.almenscorner.intune-upload.processors/IntuneAppUploader
    Arguments:
      app_file: "%flat_pkg_path%"
      assignment_info:
        - group_id: 1cf8b79a-f545-4e84-98d7-154e2efec27a # Intune - Library Kiosk iMacs
          intent: Required
      bundleId: "%bundle_id%"
      bundleVersion: "%bundle_version%"
      description: "%description%"
      displayname: "%NAME%"
      icon: "%icon_path%"
      ignore_version_detection: false
      minimumSupportedOperatingSystem: v26_0
      publisher: "%publisher%"
      sync_assignments: true
