---
Identifier: com.github.woodleighschool.pkg.Sophos

Input:
  NAME: Sophos
  REVISION: "1"

Process:
  - Processor: Unarchiver
    Arguments:
      archive_path: "%RECIPE_DIR%/Assets/SophosInstall.zip"
      destination_path: "%RECIPE_CACHE_DIR%/unzip"
      purge_destination: true

  - Processor: PkgRootCreator
    Arguments:
      pkgdirs:
        Library/Application Support/Woodleigh/Sophos: "0755"
      pkgroot: "%RECIPE_CACHE_DIR%/%NAME%/ROOT"

  - Processor: Copier
    Arguments:
      destination_path: "%pkgroot%/Library/Application Support/Woodleigh/Sophos"
      overwrite: true
      source_path: "%RECIPE_CACHE_DIR%/unzip"

  - Processor: PlistReader
    Arguments:
      info_path: "%RECIPE_CACHE_DIR%/unzip/Sophos Installer.app/Contents/Info.plist"
      plist_keys:
        CFBundleShortVersionString: version
        LSMinimumSystemVersion: minimum_os_version

  - Processor: VariableSetter
    Arguments:
      version: "%version%-%REVISION%"

  - Processor: PkgCreator
    Arguments:
      pkg_request:
        chown:
          - group: wheel
            path: Library/Application Support/Woodleigh/Sophos
            user: root
        id: com.github.woodleighschool.pkg.Sophos
        options: purge_ds_store
        pkgname: "%NAME%-%version%"
        version: "%version%"

  - Processor: PathDeleter
    Arguments:
      path_list:
        - "%RECIPE_CACHE_DIR%/unzip"
        - "%RECIPE_CACHE_DIR%/%NAME%"
